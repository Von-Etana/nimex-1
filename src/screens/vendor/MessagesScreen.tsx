import React from 'react';
import { ChatScreen } from '../ChatScreen';

export const MessagesScreen: React.FC = () => {
  // Render the ChatScreen directly within the vendor layout
  return <ChatScreen />;
};
